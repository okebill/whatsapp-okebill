require('dotenv').config();

module.exports = {
  host: 'localhost',
  user: 'root',
  password: '',
  database: 'wabot_api',
  waitForConnections: true,
  connectionLimit: 10,
  queueLimit: 0
}; 